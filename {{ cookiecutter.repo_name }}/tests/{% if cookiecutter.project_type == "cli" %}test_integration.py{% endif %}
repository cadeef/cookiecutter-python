import pytest
from click.testing import CliRunner

import {{ cookiecutter.python_module_name }}.cli

@pytest.fixture
def cli():
    """
    Returns an instance of click.testing.CliRunner
    """
    return CliRunner()
