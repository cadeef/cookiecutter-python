from typer.testing import CliRunner

from {{ cookiecutter.python_module_name }}.cli import app

cli = CliRunner()


def test_version() -> None:
    result = cli.invoke(app, ["--version"])

    assert result.exit_code == 0
    assert result.output.strip() == "{{ cookiecutter.python_module_version }}"
